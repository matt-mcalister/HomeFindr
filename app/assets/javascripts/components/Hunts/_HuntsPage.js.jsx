const HuntsPage = ({ hunts }) => {
  return (
    <div className="page-content hunts">
      // make new hunt
      // manage each hunt
        // add/delete users
        // change name
        // delete hunt
    </div>
  )
}
